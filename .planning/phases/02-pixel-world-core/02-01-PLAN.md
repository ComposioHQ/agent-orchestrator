---
phase: 02-pixel-world-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/Dashboard.tsx
  - packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx
  - packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx
  - packages/web/src/components/pixel-dashboard/SessionSprite.tsx
  - packages/web/src/components/pixel-dashboard/scene-model.ts
  - packages/web/src/components/pixel-dashboard/__tests__/scene-model.test.ts
  - packages/web/src/components/pixel-dashboard/__tests__/PixelDashboardView.test.tsx
autonomous: true
requirements:
  - SCENE-01
  - SCENE-02
user_setup: []
must_haves:
  truths:
    - Pixel mode renders one deterministic world entity per dashboard session using `DashboardSession.id` as the stable identity anchor.
    - The world layout stays structurally stable across live refreshes by using fixed project districts, fixed attention neighborhoods, and stable per-neighborhood slot assignment.
    - Session urgency is expressed from the existing `getAttentionLevel(...)` semantics through scene cues in the renderer rather than a new backend contract or Phase 3 detail surface.
  artifacts:
    - packages/web/src/components/pixel-dashboard/scene-model.ts
    - packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx
    - packages/web/src/components/pixel-dashboard/SessionSprite.tsx
  key_links:
    - packages/web/src/components/Dashboard.tsx passes shared dashboard sessions into the Phase 2 pixel scene without forking the live contract.
    - packages/web/src/components/pixel-dashboard/scene-model.ts derives districts and entity slots from existing project and attention semantics.
    - packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx replaces the Phase 1 preview with the real DOM-based world renderer.
---

<objective>
Build the deterministic world model and DOM scene renderer that turn shared dashboard sessions into stable pixel-world entities.

Purpose: Deliver the Phase 2 scene foundation without introducing a new graphics stack, backend contract, or Phase 3 inspection/action scope.
Output: A real pixel world renderer with fixed project districts, stable attention neighborhoods, always-visible session entities, and urgency cues derived from the current dashboard semantics.
</objective>

<execution_context>
@/Users/opera_user/.codex/get-shit-done/workflows/execute-plan.md
@/Users/opera_user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pixel-world-core/02-CONTEXT.md
@.planning/phases/02-pixel-world-core/02-RESEARCH.md
@.planning/phases/02-pixel-world-core/02-VALIDATION.md
@.planning/phases/01-shared-dashboard-foundation/01-02-SUMMARY.md
@packages/web/src/components/Dashboard.tsx
@packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx
@packages/web/src/lib/types.ts
@packages/web/src/hooks/useSessionEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive a deterministic world model from shared dashboard state</name>
  <files>packages/web/src/components/pixel-dashboard/scene-model.ts, packages/web/src/lib/types.ts, packages/web/src/components/pixel-dashboard/__tests__/scene-model.test.ts</files>
  <action>Create a pure scene-model module that converts visible sessions and projects into fixed district rectangles, attention neighborhoods, and stable session entity slots. Use `session.id` as the durable entity key, reuse the existing `getAttentionLevel(...)` semantics, and keep single-project mode as the same district schema rather than a separate algorithm. Sort and slot sessions deterministically so membership refreshes do not reshuffle the world unless the actual project or attention placement changes.</action>
  <verify>pnpm --filter @composio/ao-web test -- scene-model</verify>
  <done>The renderer has a testable pure world model that keeps project districts and session entities stable across ordinary live refreshes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Phase 1 preview with a DOM-based world scene</name>
  <files>packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx, packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx, packages/web/src/components/pixel-dashboard/SessionSprite.tsx</files>
  <action>Replace the bounded Phase 1 lane preview with the real Phase 2 pixel scene using absolute-positioned DOM elements and CSS transforms, not canvas or a game engine. Render fixed project districts, attention neighborhoods, stable session sprites, and always-visible labels so operators can scan the world at normal dashboard densities. Keep shell chrome and non-scene controls outside the renderer, and keep the implementation scoped to the world itself rather than adding drawers, action buttons, or parity detail panels.</action>
  <verify>pnpm --filter @composio/ao-web test -- pixel-dashboard</verify>
  <done>Pixel mode shows a real 2D world scene inside the existing shell seam, and it does so with the researched DOM-based approach instead of introducing a new rendering stack.</done>
</task>

<task type="auto">
  <name>Task 3: Encode urgency through shared attention semantics in-world</name>
  <files>packages/web/src/components/pixel-dashboard/SessionSprite.tsx, packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx, packages/web/src/components/pixel-dashboard/__tests__/PixelDashboardView.test.tsx</files>
  <action>Map the existing attention levels into readable world cues such as aura, ground ring, archive placement, and neighborhood placement so urgency can be recognized without copying legacy card UI into the scene. Keep visual language bounded to Phase 2 by avoiding new action affordances, PR drill-down, or explicit Phase 3 detail parity. Ensure done or terminal sessions move into a quieter archive area inside the district so urgent work stays visually prominent.</action>
  <verify>pnpm --filter @composio/ao-web test -- pixel-dashboard scene-model</verify>
  <done>The world communicates urgency using the same underlying semantics as the legacy dashboard while preserving a readable scene-first presentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @composio/ao-web test -- pixel-dashboard scene-model`
- [ ] `pnpm --filter @composio/ao-web typecheck`
- [ ] Manually confirm district positions remain stable when live session membership refreshes
- [ ] Manually confirm pixel mode still uses the shared dashboard shell and shared live session contract
</verification>

<success_criteria>

- One stable scene entity is rendered for each visible worker session
- Project districts and attention neighborhoods are deterministic and do not drift on ordinary refresh
- Urgency is recognizable from shared attention semantics inside the world itself
- The implementation stays within Phase 2 scope: DOM scene rendering only, no new backend contract, no Phase 3 detail/action parity
  </success_criteria>

<output>
After completion, create `.planning/phases/02-pixel-world-core/02-01-SUMMARY.md`
</output>
