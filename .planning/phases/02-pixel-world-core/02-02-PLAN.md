---
phase: 02-pixel-world-core
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/web/src/components/Dashboard.tsx
  - packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx
  - packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx
  - packages/web/src/components/pixel-dashboard/camera.ts
  - packages/web/src/components/pixel-dashboard/selection.ts
  - packages/web/src/components/pixel-dashboard/__tests__/camera.test.ts
  - packages/web/src/components/pixel-dashboard/__tests__/selection.test.ts
  - packages/web/src/components/pixel-dashboard/__tests__/PixelDashboardView.test.tsx
autonomous: true
requirements:
  - SCENE-03
  - SCENE-04
user_setup: []
must_haves:
  truths:
    - Pixel mode supports bounded pan and zoom without breaking shell access or resetting on routine live updates.
    - Selection is stored and resolved by `session.id`, remains stable across session refreshes, and is decoupled from automatic camera recentering.
    - If the selected entity leaves the viewport, pixel mode keeps selection active and shows a locator cue instead of snapping the camera or clearing state.
  artifacts:
    - packages/web/src/components/pixel-dashboard/camera.ts
    - packages/web/src/components/pixel-dashboard/selection.ts
    - packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx
  key_links:
    - packages/web/src/components/Dashboard.tsx owns `selectedSessionId` so later Phase 3 detail surfaces can reuse the same selection contract.
    - packages/web/src/components/pixel-dashboard/camera.ts clamps viewport movement against deterministic world bounds.
    - packages/web/src/components/pixel-dashboard/selection.ts computes selected entity lookup and offscreen locator state from id-based scene data.
---

<objective>
Add bounded camera navigation and stable id-based scene selection on top of the Phase 2 world renderer.

Purpose: Make the world usable for real navigation and session focus while preserving the shared shell architecture and live-update stability.
Output: Drag-to-pan and wheel/pinch-style zoom bounds, pixel-mode initial framing rules, hoisted single-session selection, and offscreen locator behavior that survives live updates.
</objective>

<execution_context>
@/Users/opera_user/.codex/get-shit-done/workflows/execute-plan.md
@/Users/opera_user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pixel-world-core/02-CONTEXT.md
@.planning/phases/02-pixel-world-core/02-RESEARCH.md
@.planning/phases/02-pixel-world-core/02-VALIDATION.md
@.planning/phases/02-pixel-world-core/02-01-PLAN.md
@packages/web/src/components/Dashboard.tsx
@packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx
@packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx
@packages/web/src/hooks/useSessionEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure camera math and bounded scene navigation</name>
  <files>packages/web/src/components/pixel-dashboard/camera.ts, packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx, packages/web/src/components/pixel-dashboard/__tests__/camera.test.ts</files>
  <action>Create pure camera helpers for initial framing, zoom clamps, pan bounds, and viewport-visible rect math, then wire them into the world scene with direct drag-to-pan and bounded zoom controls. Reset framing only on explicit mode or project-scope transitions, not on ordinary session updates, and keep the scene inside a predictable viewport that does not interfere with the shared shell. Stay within the researched DOM model and avoid adding inertial movement, canvas abstractions, or separate single-project camera logic.</action>
  <verify>pnpm --filter @composio/ao-web test -- camera pixel-dashboard</verify>
  <done>Operators can navigate the world with bounded pan and zoom, and camera behavior remains stable across normal live data churn.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist single-session selection and keep it stable across live updates</name>
  <files>packages/web/src/components/Dashboard.tsx, packages/web/src/components/pixel-dashboard/PixelDashboardView.tsx, packages/web/src/components/pixel-dashboard/selection.ts, packages/web/src/components/pixel-dashboard/__tests__/selection.test.ts</files>
  <action>Hoist `selectedSessionId` above the pixel body so selection state is owned by the shared dashboard layer and can feed later Phase 3 surfaces without another refactor. Resolve selected entities by id against the current sessions array, preserve the selection through snapshot resets and membership refreshes whenever the session still exists, and keep Phase 2 to single-select only. Do not store session-object references or couple selection changes to automatic camera recentering.</action>
  <verify>pnpm --filter @composio/ao-web test -- selection useSessionEvents pixel-dashboard</verify>
  <done>Selection persists by session identity through live updates and is ready to be consumed by later non-canvas detail surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Add interaction boundaries and offscreen selected-session cues</name>
  <files>packages/web/src/components/pixel-dashboard/PixelWorldScene.tsx, packages/web/src/components/pixel-dashboard/selection.ts, packages/web/src/components/pixel-dashboard/__tests__/PixelDashboardView.test.tsx, packages/web/src/components/pixel-dashboard/__tests__/selection.test.ts</files>
  <action>Add the Phase 2 interaction rules around the selected entity: clicking an entity highlights and pins it, leaving it offscreen shows a locator cue, and normal scene updates do not clear or snap to it. Keep controls and details outside the world itself so camera interaction does not break shell access, and stop short of Phase 3 by not adding a details drawer, action controls, or richer inspection surface. Verify that world interaction remains readable on common desktop viewport sizes within the current shell layout.</action>
  <verify>pnpm --filter @composio/ao-web test -- pixel-dashboard selection camera</verify>
  <done>The scene supports stable single-select behavior with explicit offscreen feedback and bounded interactions that preserve the operator’s camera position.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @composio/ao-web test -- pixel-dashboard camera selection useSessionEvents`
- [ ] `pnpm --filter @composio/ao-web typecheck`
- [ ] Manually confirm pan/zoom remains usable while the shared shell controls stay accessible
- [ ] Manually confirm selected sessions remain selected through live updates and show a locator cue when offscreen
</verification>

<success_criteria>

- Operators can pan and zoom the world within useful bounds without breaking access to shared shell controls
- Scene framing only resets on explicit mode or scope transitions, not on routine live updates
- Session selection remains stable by id and survives refreshes while the session still exists
- Offscreen selected entities remain selected and visible through a locator cue rather than automatic recentering or clearing
  </success_criteria>

<output>
After completion, create `.planning/phases/02-pixel-world-core/02-02-SUMMARY.md`
</output>
