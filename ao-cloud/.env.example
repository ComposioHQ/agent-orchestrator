AO_CLOUD_LISTEN_ADDR=127.0.0.1:3010
AO_CLOUD_PUBLIC_URL=http://127.0.0.1:3010
AO_WEB_PUBLIC_URL=http://127.0.0.1:5174
AO_SANDBOX_PROVIDER=daytona

AO_DATABASE_URL=postgresql://localhost/ao_cloud_dev?sslmode=disable
AO_DATABASE_DIRECT_URL=postgresql://localhost/ao_cloud_dev?sslmode=disable
AO_CLOUD_TEST_DATABASE_URL=postgresql://localhost/ao_cloud_test?sslmode=disable

AO_SUPABASE_URL=https://PROJECT_REF.supabase.co
AO_SUPABASE_ANON_KEY=

AO_DAYTONA_API_URL=https://app.daytona.io/api
AO_DAYTONA_API_KEY=
AO_DAYTONA_TARGET=us
AO_DAYTONA_WORKER_SNAPSHOT=daytona-large

AO_FLY_API_URL=https://api.machines.dev/v1
AO_FLY_API_TOKEN=
AO_FLY_APP=
AO_FLY_REGION=iad
# Optional override; AO defaults to its published stable Fly worker image.
AO_FLY_WORKER_IMAGE=registry.fly.io/ao-workers-nihal-2026:stable

# 32 random bytes encoded as 64 hexadecimal characters.
AO_ENCRYPTION_KEY=
AO_WORKER_SIGNING_KEY=

# Development only. Production uses a GitHub App-backed credential source.
AO_GITHUB_AUTH_MODE=local-gh
# Temporary hosted fallback: a fine-grained repository token kept only in the
# control plane. Omit once the GitHub App is configured.
AO_GITHUB_TOKEN=

# Development-only upload path when the Daytona snapshot does not yet contain
# ao-worker. Production worker snapshots start the baked-in worker directly.
AO_WORKER_BINARY_PATH=
