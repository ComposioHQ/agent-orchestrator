interface:
  display_name: "AO Desktop Dev"
  short_description: "Run AO Electron against a safe local DB clone"
  default_prompt: "Use $ao-desktop-dev to clone the current AO database, launch this checkout’s Electron app against the dev copy, and verify the requested flow."
